/*
 * This file was generated by the Gradle 'init' task.
 *
 * This project uses @Incubating APIs which are subject to change.
 */

plugins {
    id 'core.java-application-conventions'
}

dependencies {
    implementation 'org.apache.commons:commons-text:1.10.0'
    implementation project(':utilities')
    compileOnly 'jakarta.servlet:jakarta.servlet-api:6.0.0'
    // added to test pinned vs unpinned
    implementation 'io.dropwizard:dropwizard-core:(2.1.0,2.1.5]'
    // Intentionally vulnerable (CVE-2021-44228 Log4Shell); for scanner/demo only — remove or upgrade before production
    implementation 'org.apache.logging.log4j:log4j-core:2.14.1'
    // Not in Dropwizard SBOM tree; intentionally vulnerable (CVE-2023-1370 json-smart parser DoS); scanner/demo only
    implementation 'net.minidev:json-smart:2.4.7'
    // Unrelated to web stack; intentionally vulnerable (CVE-2023-50868 dnsjava DNSSEC); scanner/demo only
    implementation 'dnsjava:dnsjava:3.5.1'
    // Unlikely in Dropwizard deps; intentionally vulnerable (CVE-2021-21351 XStream deserialization); scanner/demo only
    implementation 'com.thoughtworks.xstream:xstream:1.4.16'

}
version '1.0.4'
application {
    // Define the main class for the application.
    mainClass = 'core.app.App'
}
